# Build & deps
dist/
.astro/
node_modules/
pnpm-lock.yaml
skills-lock.json

# Project docs — must stay byte-exact (CONTENT.md is the verbatim copy source of truth).
# Never let Prettier reformat these.
PLAN.md
UX-REPORT.md
CONTENT.md
implementation-prompt.md
PROGRESS.md
PHASE-CHECK.md
DECISIONS.md

# Design-token source of truth — the @theme block is a VERBATIM copy of PLAN §4
# (uppercase hexes, aligned comments). Prettier would lowercase hexes and re-wrap,
# breaking byte-for-byte fidelity with the plan. Keep it out of auto-format.
src/styles/global.css

# Static assets served as-is
public/
